// TODO(v3): rewrite this test against the v3 carousel internals. The v3
// implementation uses different overlay routing for arrow keys and changed
// the indicator semantics, so the v1 finders no longer apply.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('skipped — pending v3 rewrite', () {}, skip: 'pending v3 rewrite');
}
