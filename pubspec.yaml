name: cl_components
description: CL UI Components for Generazione AI projects
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.8.0
  flutter_svg: ^2.1.0
  google_fonts: ^6.2.1
  responsive_framework: ^1.5.1
  intl: ^0.20.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
